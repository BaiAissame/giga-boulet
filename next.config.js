/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['yvt0xuhvxht9er2d.public.blob.vercel-storage.com'], 
  },
};

module.exports = nextConfig;
